<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Rotate the ImageView -->
    <objectAnimator
        android:valueFrom="0"
        android:valueTo="180"
        android:propertyName="rotationY"
        android:duration="1000" />

    <!-- Change the alpha attribute using a ValueAnimator -->
    <objectAnimator
        android:duration="1000"
        android:propertyName="alpha"
        android:valueFrom="1"
        android:valueTo="0"
        android:valueType="floatType"
        android:repeatCount="0">

        <keyframe
            android:fraction="0.5"
            android:value="0" />

        <keyframe
            android:fraction="1"
            android:value="1" />
    </objectAnimator>

</set>